<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:viewmodels="clr-namespace:ChatUI.ViewModels" x:DataType="viewmodels:ChatPageViewModel"
             x:Class="ChatUI.Views.ChatInputBarView">
    <Grid RowSpacing="0"
          ColumnSpacing="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Frame CornerRadius="8">
            <Entry x:Name="chatTextInput"
               Text="{Binding TextToSend}"
               Margin="-16"
               TextColor="Black"
               Keyboard="Chat"
               HorizontalOptions="FillAndExpand"
               PlaceholderColor="LightGray"
               Placeholder="Type message"
               Completed="Handle_Completed"
               BackgroundColor="White"/>
        </Frame>
        <Label Text="Send"
               Grid.Row="0"
               Grid.Column="1"
               VerticalTextAlignment="Center">
            <Label.GestureRecognizers>
                <TapGestureRecognizer
                    Command="{Binding OnSendCommand}"
                    Tapped="Handle_Completed"/>
            </Label.GestureRecognizers>
        </Label>
    </Grid>
</ContentView>